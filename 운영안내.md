# 디코딩 미션 워크숍

## 구현한 기능
- 강사 로그인과 워크숍별 관리자 권한
- 1~30개 팀 생성, 참가자 입장 QR 코드, 닉네임과 역할 선택
- 팀당 현장전문가 1명, 해독전문가 여러 명
- 참가자 접속 상태와 팀별 진행률 표시(2초 간격)
- 첨부 구성표와 같은 9개 라운드, 9종 장치
- 제한 시간 1~60분, 시작·일시정지·이어하기·종료
- 라운드당 오답 3회, 서버 시간 기준 시간 초과 판정
- 장치 해제 100점, 전체 해제 시 남은 초 보너스, 라운드 간 점수 누적
- 오답·정답·전체 성공 효과음과 축하 애니메이션
- 모바일 가로 화면 대응, 웹 해독 매뉴얼과 인쇄
- 점수 CSV 저장, 데이터베이스에 진행 상태 저장

## 운영 흐름
1. 강사가 첫 화면에서 로그인하고 팀 수·제한 시간·라운드를 선택하여 워크숍을 생성합니다.
2. 생성 후 주소를 보관합니다. 같은 주소에서 관제를 다시 열 수 있습니다.
3. 참가자가 QR로 접속해 닉네임·팀·역할을 선택합니다.
4. 해독전문가는 매뉴얼을 열거나 인쇄본을 사용합니다.
5. 강사가 라운드를 시작합니다. 다음 라운드를 선택한 뒤 설정 적용, 라운드 시작 순으로 진행합니다.

## 현재 전달 상태
로컬 앱의 제작, 타입 검사, 배포용 빌드와 서버 기능 검증은 완료했습니다.
온라인 게시를 위한 Git 기록 저장은 환경의 폴더 쓰기 차단 때문에 완료하지 못했습니다.
등록된 Sites 프로젝트는 있으나 게시된 주소는 없습니다. 외부 휴대폰에서 QR로 이용하려면 게시와 참가자 접근 허용이 필요합니다.

## 매뉴얼 호환성
기존 기업의 로고와 이미지를 복제하지 않은 별도 웹앱입니다.
라운드 구성은 첨부 표를 따릅니다. 상형문자·미로·음표·언어유희·교집합은 이 앱 전용 표와 지도를 사용합니다.
첨부 이미지 매뉴얼 대신 앱의 /manual 페이지를 사용해야 정답 규칙이 일치합니다.

## 검증 범위
- 생성 장치 1,560개의 해제 가능성과 미로 경로 연결 검증
- 워크숍 생성, 익명 참가자 입장, 중복 현장전문가 방지
- 참가자의 관리자 동작·해독전문가의 장치 조작 차단
- 시작, 정지, 이어하기, 종료, 3회 오답, 시간 초과
- 전체 성공 보너스, 점수 중복 지급 방지, 점수 누적
- 동시 오답 요청, 이전 라운드 요청 거절, 재접속
- /, /play, /manual 화면 응답과 타입 검사, 빌드 성공
실물 휴대폰의 터치·음량·브라우저별 화면 및 대규모 동시 접속 부하 시험은 별도로 하지 않았습니다.
WebMCP 설정 도구를 추가했지만 이 환경에서 브라우저 계약 검증은 하지 못했습니다.

## 개발 환경
Node.js 22.13 이상과 npm이 필요합니다.
프로젝트 폴더에서 npm run install:ci 후 npm run dev로 실행합니다.
D1 데이터베이스를 사용하는 앱이므로 정적 파일 호스팅만으로는 작동하지 않습니다.
Sites 프로젝트 식별자는 .openai/hosting.json에 저장되어 있습니다. 기존 프로젝트를 재사용해야 합니다.
생성된 Drizzle 마이그레이션은 drizzle/0000_tearful_hellcat.sql입니다.
로컬 데이터베이스 준비는 README.md의 Local D1 migrations 절차를 따릅니다.
관리자 인증은 Sites의 ChatGPT 로그인을 사용합니다. 공개 게시 시 참가자는 별도 로그인 없이 QR로 입장하도록 구현되어 있습니다.
